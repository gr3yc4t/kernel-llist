#include <stdlib.h>
#include <stdio.h>
#include "animal_list.h"

int main(int argc, char *argv[])
{
    if (argc != 1)
    {
        printf("USAGE: %s\n", argv[0]);
        return -1;
    }

    return 0;
}
